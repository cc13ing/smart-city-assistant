# AutoCityIntro — AGENTS.md

本文件用于约束本仓库的“开发范围”和“最简实现”原则，供人类开发者与自动化 Agent 协作时遵循。目标是：在不引入额外复杂抽象的前提下，稳定迭代一个可用的智能城市助手。

## 项目定位

- 一个本地运行的智能城市助手：聊天 → 识别意图 → 调用地图/POI/路线等服务 → 输出可执行建议与可视化结果
- 技术栈固定：Python 3.12 + FastAPI + LangGraph/LangChain + 高德地图 API + 纯静态前端（Vanilla JS）
- 数据以本地文件为主：会话、画像、行程落在 `data/` 下（JSON/SQLite checkpoint）

## 运行方式（开发）

- 一键启动：`python run_all.py`
- 端口约定：
  - Web UI：`http://localhost:7003`（`web_app.py`）
  - MCP/工具服务：`http://localhost:7001`（`mcp_server.py`）
- 环境变量：使用 `.env`（建议仅本地保存，不要提交真实密钥）

## 代码结构（不要引入新层）

- `web_app.py`：Web 界面托管 + API 网关（面向浏览器）
- `mcp_server.py`：MCP 工具服务（面向 Agent/工具调用）
- `graph/`：LangGraph 编排、状态、节点、子图（智能逻辑的“主入口”）
- `services/`：可复用的业务能力与外部 API 访问（路线、POI、隐私、画像、缓存等）
- `static/`：纯静态前端资源（不要引入 React/Vue/构建链）
- `data/`：本地数据目录（会话/画像/行程/缓存/Checkpoint）

原则：优先把“新能力”放进现有目录，不新增“框架层/抽象层/通用平台层”。如果一段逻辑只服务一个入口，就直接写在对应入口文件里；如果会被复用，再下沉到 `services/`。

## 开发范围（In Scope）

- 修复 bug、提升稳定性（错误处理、超时、重试、缓存命中、SSE 流式输出体验）
- 优化对话与意图识别效果（Prompt、解析器、LangGraph 节点编排、回复模板）
- 地图与行程能力增强（路线规划、周边推荐、行程生成/保存/分享、离线缓存策略）
- 隐私与权限改进（Owner 校验、数据最小化、未成年人模式、位置脱敏）
- Web UI 的小步改进（交互/布局/状态同步），保持纯静态实现

## 非目标（Out of Scope）

- 大规模重构：不做“全量架构升级/模块重写/换框架”
- 引入前端工程化：不引入 React/Vue/打包器/TypeScript（除非用户明确要求）
- 引入复杂基础设施：不引入数据库迁移、消息队列、分布式缓存等（除非用户明确要求）
- 过度抽象：不新增“通用工具层/基类体系/领域框架/插件系统”

## 最简开发原则（必须遵守）

- 小步提交：一次变更只解决一个问题；优先最小 diff
- 少依赖：除非明确必要，不新增第三方依赖；能用标准库就用标准库
- 少概念：不造新名词、不新建“manager/factory/engine”一类泛化模块
- 少魔法：避免隐式全局状态与复杂装饰器链，优先显式参数传递
- 复用现有能力：先查 `services/`、`graph/` 是否已有类似实现，再新增

## 定位链路约束（重要）

- 前端定位必须依赖浏览器原生 `navigator.geolocation`
- 不使用 IP 定位兜底
- 不集成高德 JS Geolocation 插件

任何涉及定位的改动都必须保持上述约束不变。

## 安全与隐私（必须）

- 不在前端暴露服务端密钥；区分 `AMAP_JS_KEY` 与后端 `AMAP_API_KEY` 的用途
- 不在日志/接口响应中输出密钥、Token、用户完整经纬度（除非已脱敏且用户明确同意）
- `data/` 下的会话/画像/行程默认视为敏感数据：导出/分享必须走现有分享逻辑并做脱敏

## Agent 工作方式（协作约定）

- 改代码前先定位入口：是 Web 交互问题就看 `static/` 与 `web_app.py`；是能力问题就看 `services/`；是智能编排就看 `graph/`
- 不做“顺手重构”；如果发现结构性问题，先提出建议与最小修复方案，由用户决定是否升级改造
- 修改完成后必须自检：
  - 静态检查：确保导入、类型/字段名一致
  - 本地运行：能正常启动 `run_all.py`，关键路径可用（聊天/路线/POI 至少一条链路）

